# 21 — Graded AI-overlay improvement loop (review)

Status: resolved
Type: research
Track: Future (agent overlay post-training / eval industrialisation)
Blocked by: 11, 10

Activation gate: owner authorised the written review on 6 August 2026. Do not
introduce cloud training providers, API keys, or RL that can enforce
transfers/chips. Any provider (including Prime Intellect Lab / Verifiers-style
environments) needs a fresh ADR amending ADR-0021/0024.

## Context

The lab’s scarce asset is sealed, point-in-time rewards and fail-closed rubrics
for the evidence/challenger overlay — not raw GPU. Ticket 11 schedules the
subscription sol path; ADR-0024 declined a metered API arm. Platforms that turn
**dataset + harness + rubric** into eval → synthetic data → optional LoRA
training (e.g. Prime Intellect Lab / open Verifiers environments) may be a fit
for owning a graded improvement loop on the AI overlay, analogous to narrow
subagent post-training elsewhere.

This ticket is a **review**, not an implementation mandate.

## Scope

- Inventory existing sealed rewards: agent-eval golden cases, evidence/challenger
  schemas, policy gates, fork fixtures, GDR degrade contracts.
- Sketch one candidate environment (dataset + harness + rubric) from those
  artefacts; reward must stay proposal-only (legality, citations, unopposed
  adjustments, sealed outcome deltas where allowed).
- Compare options: local Verifiers-only eval loop vs hosted training/inference
  (Prime Intellect or other); cost, secrets, reproducibility, ADR impact.
- Recommend go / no-go / defer, and the smallest experiment if go.

## Done when

- A written review under `docs/` (or this ticket’s Answer) states: fit to the
  lab architecture, phase risks, ADR requirements, and a single recommended
  next experiment or an explicit deferral.
- No live `run_gameweek` path changes; deterministic core untouched.

## Boundaries

LLMs propose only. No optimiser/rules replacement by RL. No secrets in Git or
prompts. No package installs or cloud account binding without owner sign-off in
session. Do not scrape or invent FPL outcomes for rewards.

## Comments

> *This was generated by AI during triage.*

Owner (6 August 2026) asked to park a future ticket to review “owning a graded
improvement loop on the AI overlay” after discussing Prime Intellect as possible
alpha for the agent overlay layer.

## Answer

**Defer** hosted training / provider binding (including Prime Intellect). Fit is
real for a proposal-only Verifiers-style env over existing sealed rewards, but
early live 2026/27 GDRs and a local graded suite should come first.

Full review: `docs/reviews/2026-08-graded-ai-overlay-improvement-loop.md`

Summary:

- Inventory covers golden agent/evidence cases, schema validators, agent_arm
  gates, fork runners, JSONL traces, and T-90m / forced-timeout GDR degrade.
- Candidate env: dataset = hash-bound requests; harness = agent_arm / scheduled
  overlay; rubric = schema, citations, fail categories, optional post-freeze
  sealed deltas — never live enforcement.
- Prefer local offline graded eval (no new deps) when re-opened; Verifiers
  package and hosted LoRA need owner install / ADR-0021+0024 amendments.
- No `run_gameweek` or deterministic-core changes from this ticket.
